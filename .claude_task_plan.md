# Docker Publish CI Repair Plan

## Objective
Identify and fix the failure in the `docker-publish` GitHub Actions workflow, then verify the successful rerun.

## Impacted Files
- `.github/workflows/docker-publish.yml`
- `Dockerfile` (only if the build log identifies an image-build failure)

## Implementation Steps
1. Retrieve the failed workflow log for run `31785975369`. (complete)
2. Apply the smallest correction to the workflow or Docker build. (complete)
3. Validate the edited configuration and verify the rerun after push. (in progress)

## Verification Strategy
Use the failed Actions log as the root-cause source. Validate YAML and Dockerfile syntax locally where possible; GitHub Actions is authoritative for the multi-architecture push.

## Findings
- The workflow logs into GHCR with package write permission, builds `linux/amd64` and `linux/arm64`, and passes Supabase public build arguments.
- The Dockerfile has separate dependency, build, and runtime stages; exact failure cause requires the Actions log.
- The full build succeeds on both architectures. Publishing alone fails because the workflow runs in `Amouriii/Omnitaps` but tags `ghcr.io/onouh/omnitaps`; the repository workflow token cannot create or write that different owner's GHCR package. Derive the image owner from `github.repository_owner` so the token pushes to its own package namespace.
- The corrected workflow parses successfully as YAML and `git diff --check` passes. The working tree also contains an unrelated untracked `lib/network/` directory, which must not be staged or changed.
- Staged-diff validation passed, and the one-line workflow repair was committed as `3210260` (`Fix GHCR image namespace`). The next step is to push it and inspect the new Docker publish run.
- Commit `3210260` was pushed to `origin/main`, triggering a fresh Docker publish job. Await the GitHub Actions result before removing this temporary plan. The untracked `lib/network/` directory remains untouched.
